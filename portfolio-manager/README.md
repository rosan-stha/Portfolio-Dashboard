# portfolio-manager

Contains the Portfolio Manager frontend app.

## Source of truth

`portfolio-manager/project/Portfolio Manager.html` is the single-file production app — a self-contained React application (CDN React + Babel standalone, no build step). Edit it directly.

There is no bundler, no `npm run build`, and no component pipeline. The file is the source.
